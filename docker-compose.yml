services:
  app:
    env_file:
      - .env.local
    build: .
    container_name: expense-tracker-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true